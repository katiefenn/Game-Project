
describe("The Game is an object that encapsulates all of the functionality and", function () {

/*    it("should run a game module when started", function () {
        var moduleMock = {run: jasmine.createSpy('run')},
            game = new Game({module: moduleMock});

        game.start();
        expect(moduleMock.run).toHaveBeenCalled();
    });*/
});